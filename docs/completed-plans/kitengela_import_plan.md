# Add Products and Stocks from Kitengela1.csv

This plan outlines the SQL migration required to populate the database with data from `Kitengela1.csv` for the tenant "Prestine Homes" and branch "Kitengela".

## Context
Based on the database schema and your requirements:
1. **Tenant ID**: `870a2a76-4a11-4b6f-a537-ee71d4f82037` ("Prestine Homes")
2. **Branch ID**: `9b4d5a65-889f-40c0-919c-921c4eee91d8` ("Kitengela")
3. **Item Groups**: Global/Tenant-wide (`tenant_id` is set, `branch_id` is left `NULL`).
4. **Products**: Global/Tenant-wide (`tenant_id` is set, `branch_id` is left `NULL`). Products will reference the generated `item_group_id`.
5. **Stock**: Branch-specific (`tenant_id` and `branch_id` are both set). Stock records will link the product to the Kitengela branch.

## User Review Required

> [!IMPORTANT]
> The CSV contains a column `group selling price`. For some groups (e.g., `FGB44`), this value is constant across all products (1650). However, for `Accessories`, the value varies per product (e.g., Adhesive Ceramic is 450, Adhesive Granite is 600). 
> 
> **My assumption**: I will set the individual product's `base_price` (and `commission_value` if present) to the values on their respective CSV row, rather than applying a single uniform price to the whole item group. Is this correct?

## Open Questions

> [!WARNING]
> Do you want the CSV processing to be done via a one-off SQL script generated by the agent (which we can execute directly via Supabase MCP), or do you want me to write a Dart script to do this programmatically? Using a raw SQL script run via Supabase MCP is the most direct approach.

## Proposed Changes

We will execute a SQL script via Supabase MCP (or write a migration file) that does the following:

1. **Insert Item Groups**
   Extract all unique item groups from the CSV (Accessories, Basins, BLO44/FGE44, etc.) and insert them into `public.item_groups` with:
   - `tenant_id` = '870a2a76-4a11-4b6f-a537-ee71d4f82037'.
   - `default_commission_value` = CSV "group commission" (if any, grouping by unique item group).

2. **Insert Products**
   For each row in the CSV, insert a record into `public.products`:
   - `tenant_id`: '870a2a76-4a11-4b6f-a537-ee71d4f82037'
   - `name`: CSV "Name"
   - `base_price`: CSV "group selling price"
   - `cost_price`: CSV "group buying price" (if any)
   - `item_group_id`: The ID of the group created in step 1.
   
3. **Insert Stock**
   For each row in the CSV, insert a record into `public.stock`:
   - `tenant_id`: '870a2a76-4a11-4b6f-a537-ee71d4f82037'
   - `branch_id`: '9b4d5a65-889f-40c0-919c-921c4eee91d8'
   - `product_id`: The ID of the product created in step 2.
   - `quantity`: CSV "Stock"

## Verification Plan

### Manual Verification
- After the SQL migration is executed, I will run a `SELECT` query via Supabase MCP to ensure the `item_groups`, `products`, and `stock` tables contain the expected row counts for the specified tenant and branch.
